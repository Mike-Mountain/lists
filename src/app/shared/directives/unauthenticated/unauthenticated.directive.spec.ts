import { UnauthenticatedDirective } from './unauthenticated.directive';

describe('UnauthenticatedDirective', () => {
  it('should create an instance', () => {
    const directive = new UnauthenticatedDirective();
    expect(directive).toBeTruthy();
  });
});
